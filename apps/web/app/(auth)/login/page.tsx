import { PageIntro } from "@/components/page-intro";

export default function LoginPage() {
  return (
    <PageIntro
      description="Sign in with email and password to enter the dispatcher, admin, or driver workspace."
      eyebrow="Auth"
      title="Login"
    />
  );
}
