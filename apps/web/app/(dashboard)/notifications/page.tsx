import { PageIntro } from "@/components/page-intro";

export default function NotificationsPage() {
  return (
    <PageIntro
      description="Review unread operational alerts, incident updates, document results, and notification history."
      eyebrow="Alerts"
      title="Notifications"
    />
  );
}
