import { PageIntro } from "@/components/page-intro";

export default function DriverReportPage() {
  return (
    <PageIntro
      description="Mobile-first flow for drivers to report incidents with short forms, attachments, and clear status."
      eyebrow="Driver flow"
      title="Report incident"
    />
  );
}
